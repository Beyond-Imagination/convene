// @migration/shared-interfaces
// Plain TypeScript interfaces + domain event name constants.
// No class-validator decorators. No zod schemas (frontend handles form validation locally).
// Populated during the "tests first" step — see PLAN.md §9 and CLAUDE.md.

export {};
